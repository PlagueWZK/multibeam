请根据下面的不同分区特征组合,跑完分区流程(不需要测线规划流程).
- 待测分区特征组合:
   1. 一次分区特征: x,y坐标,深度矩阵,覆盖次数矩阵.  对于触发二次分区(梯度方向离散度大于0.2)的区域进行二次分区,特征是仅梯度方向
   2. 一次分区特征: 梯度方向;对每个分区采用二次分区,特征是:深度矩阵,覆盖次数矩阵

- 对于特征组合:xy-depth-coverage_then-direction,做以下调整:
  1. 二次分区触发阈值改成大于0.2就进行二次分区

- 对于特征组合:direction-only_then-depth-coverage,做以下调整:
  1. 通过梯度方向一次分区后,把在物理空间中不连通的同一分区分开为不同的分区
  2. 二次分区后,对于分区面积小于总海域面积3%的分区,将该分区合并到与它接触面最大的分区
  3. 完成2之后,仅对分区面积在总海域面积3%-5%的分区执行被包围分区合并逻辑

要求/注意:
1. 需要使用当前项目的虚拟环境运行
2. 需要显式设置终端的编码为UTF-8
3. 每种组合的输出目录命名要标明时间戳,组合类型以及起始点选择(若只是分区流程,不包括测线规划流程,则不需要标明起始点)
4. 准备新的组合时,只需要在当前分支修改
